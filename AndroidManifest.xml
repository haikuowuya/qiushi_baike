<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roboo.qiushibaike"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <application
        android:name=".QiuShiApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CYDBActivity" />
        <activity android:name=".CSDNActivity" />
        <activity android:name=".KJFMActivity" />
        <activity android:name=".ShowImgActivity" />
        <activity android:name=".UserQiuShiActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".CommentActivity" />

        <service android:name=".MyService" />
        <service android:name=".MyIntentService" />
    </application>

</manifest>